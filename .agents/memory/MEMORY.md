# Agent Memory Pointer

Canonical project facts are stored in `project-memory/*.json`.

This directory may contain navigation pointers only. Do not record competing project state, business rules, defects, permissions, or verification claims here.
